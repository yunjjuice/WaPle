<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.waple.theme.dao.ThemeDao">

    <insert id="create" parameterType="com.ssafy.waple.theme.dto.ThemeDto">
        insert into THEMES (GROUP_ID, THEME_ID, NAME, ICON)
        values (#{groupId}, (SELECT t.THEME_ID + 1 FROM THEMES t WHERE t.GROUP_ID = #{groupId} ORDER BY t.THEME_ID DESC LIMIT 1), #{name}, #{icon})
    </insert>

    <insert id="createInit" parameterType="com.ssafy.waple.theme.dto.ThemeDto">
        insert into THEMES (GROUP_ID, THEME_ID, NAME, ICON)
        values (#{groupId}, 1, #{name}, #{icon})
    </insert>

    <select id="readAll" parameterType="int" resultType="com.ssafy.waple.theme.dto.ThemeDto">
        select *
        FROM THEMES
        where GROUP_ID = #{groupId}
    </select>

    <update id="update" parameterType="com.ssafy.waple.theme.dto.ThemeDto">
        update THEMES
        set NAME = #{name},
            ICON = #{icon}
        where GROUP_ID
    </update>

    <delete id="delete" parameterType="map">
        delete
        from THEMES
        where GROUP_ID = #{groupId}
          and THEME_ID = #{themeId}
    </delete>
</mapper>
